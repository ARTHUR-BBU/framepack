{
  "name": "framepack",
  "version": "0.5.0-alpha.13",
  "private": false,
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "framepack": "dist/cli.js"
  },
  "description": "A lightweight HyperFrames creative workbench for assets, prompts, composition, and iteration.",
  "readme": "# Framepack\n\nFramepack is a programmatic video workbench for coding agents (Codex, Claude Code) and the HyperFrames render runtime. It turns rough user ideas, assets, references, and fuzzy taste words into a professional video plan.\n\nProgrammatic video (Framepack) vs Generative video (Runway/Sora): Framepack orchestrates existing assets with HTML/CSS/GSAP animations — no pixels are generated, only composed. Generative AI creates new imagery from text prompts.\n\n中文: Framepack 是程式化视频工作台。它把模糊的用户需求翻译成设计系统、视觉令牌、HTML 骨架、素材说明、composition 方案和迭代记忆。不生成画面，只编排画面。\n\nQuick start:\n\n```bash\nnpm install framepack\nnpx framepack create --idea \"A premium launch video\" --assets ./assets --output-dir ./out --project-name launch-video\nnpx framepack workbench brief --project-dir ./out/launch-video\nnpx framepack templates prompt --json\n```\n\nGenerated workbench includes: FRAMEPACK.md, ASSETS.md, HUMAN.md, STYLE.md, DIRECTION.md, COMPOSITION.md, ITERATIONS.md, DESIGN.md (auto-matched from 22 design systems), DESIGN_TOKENS.md (extracted colors and typography), ASSET_GAPS.md (blocking/optional gap analysis), index.html (HyperFrames-passable HTML skeleton).\n\nRead `FRAMEPACK.md` and `HUMAN.md` first. `COMPOSITION.md` includes Template Fusion Plan, Catalog Pre-Flight, code templates, and HyperFrames safety rules. Coding agent skills with progressive disclosure installed under `.claude/skills`.\n\nFull docs: https://github.com/ARTHUR-BBU/framepack\n",
  "files": [
    "dist",
    "examples",
    "README.md",
    "AGENTS.md",
    "docs/rebirth",
    "scripts/postinstall.mjs",
    "templates",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "keywords": [
    "video",
    "agent",
    "hyperframes",
    "compiler",
    "cli",
    "automation"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "release:gate": "node scripts/run-release-gate.mjs",
    "release:scenarios": "npm run build && node scripts/run-real-scenarios.mjs",
    "release:smoke:install": "npm run build && node scripts/run-install-smoke.mjs",
    "postinstall": "node scripts/postinstall.mjs",
    "test": "npm run build && node scripts/run-tests.mjs",
    "test:watch": "npm run build && node --watch scripts/run-tests.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hyperframes": "^0.6.40",
    "zod": "^4.4.3"
  }
}
