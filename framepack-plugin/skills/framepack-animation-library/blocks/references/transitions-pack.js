/**
 * TRANSITION EFFECTS — DO NOT USE.
 *
 * HyperFrames handles scene transitions natively. Do NOT implement
 * manual scene switching with display:hidden, opacity:0 on outgoing
 * scenes, or DIY transition effects.
 *
 * HyperFrames rules (from `hyperframes` skill):
 *   - NEVER use exit animations except on the final scene
 *   - The transition IS the exit
 *   - ALWAYS use HyperFrames' built-in transition system
 *
 * For transition configuration, see HyperFrames' transitions reference:
 *   skill_view(name="hyperframes", file_path="references/transitions.md")
 *
 * This file is preserved as a historical reference only.
 * It must NOT be loaded or executed in any HyperFrames composition.
 */
"DO NOT USE — HyperFrames handles transitions natively."
