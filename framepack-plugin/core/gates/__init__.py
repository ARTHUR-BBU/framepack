"""Gate engine compatibility package.

The first migration step exposes the new package surface while legacy readiness
checks still live in core.render_readiness. Later tasks move individual gates
behind this package one by one.
"""
