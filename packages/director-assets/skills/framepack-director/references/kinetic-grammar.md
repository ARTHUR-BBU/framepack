# 动势语法

动画要像句子，有前因和后果。

- `cause_reveal`：一个动作触发另一个信息显形。
- `echo_transform`：同一母题变形后在下一幕回归。
- `mask_portal`：遮罩成为进入下一空间的门。
- `tension_release`：先克制蓄力，再释放冲击。
- `scatter_assemble`：碎片先分散，再组装成意义。
- `follow_through`：上一动作的惯性接力给下一元素。
- `breath_punch_silence`：呼吸、重击、静止，形成节奏落差。

每条场景边界至少说明一种语法。禁止把连续淡入淡出当作完整编舞。
